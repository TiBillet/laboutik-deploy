# laboutik-deploy

Un `make install` pour les nouveaux VPS.

```bash
make help      # la liste des cibles
make install   # installation d'un VPS de production
```

Une fois la stack démarrée (`docker compose up -d`), mettre en place la
sauvegarde : **[BACKUP.md](./BACKUP.md)**.
